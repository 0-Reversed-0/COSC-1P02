import Media.*;

public class Tester
{
    
    /**
     * Classes are made up of:
     * States which are instance variables 
     * Behaviours which are the methods (constructors are glorfied methods)
     */
    
    /**
     * Ex: Turtle is a class
     * States: x: 0, y:0, pen width: 1, pen colour: black, angle: 0.0, etc.
     * Behaviour: changes states in the turtle i.e. yertle.forward(10); 
     */
    
    Classes balls = new Classes();
 
    public Tester()
    {
        balls.setX(69);
        int x = balls.getX();
        System.out.print(x);
    }
    
   
}